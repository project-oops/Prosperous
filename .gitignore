/target
# Cargo.lock is committed: this workspace ships binaries.

# Payload binaries are never tracked; the manifest says where to fetch them.
*.elf
*.bin
*.prx
*.sprx
*.pkg

# The Windows build directory, for the icon-bearing executable.
/target-win/

# Per-person Claude Code overrides; `settings.json` beside it is shared.
.claude/settings.local.json

# Editor and OS files.
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~
