# Names written as prose rather than code identifiers: a sibling project, the protocol the
# Moonlight bridge speaks and its fields. `..` keeps clippy's defaults.
doc-valid-idents = ["SELFish", "GameStream", "GfeVersion", "ENet", "XInput", ".."]
